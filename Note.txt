Environment Variables

we installed a dotenv package with npm i dotenv, we will have .env file and we will define the variables we will like to protect in there.
the env is deployed and server on the server itself.
To access the env you will write process.env.(any item you want to access)