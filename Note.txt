Paypal set up and order Paid.

in orderController, at updateOrdertopaid function get the order by findbyid method, w use the if sttatement to get the if there is an order else we throw an error. then we go paypal website and register. then copy the secret code to your .env file your paypal client id.
then we added the paypal route in our backend so we did that in our server.js