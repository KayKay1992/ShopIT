SEARCH BOX COMPONENT: 

The component will enable us to see where we can search an item without searching through the route.

we created the searchBox component where the frontend search functionality is implemented 
then we passed this component into the header component where it is displayed in the header section
Then at the homeScreen the goback button is implemented to enable use go back to homepage

