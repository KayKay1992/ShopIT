Product Carousel Implementation

In ProductController we created getTopProduct function this is whwere we sort the 3 best product.
We then registered the get topproduct route in productRoute. we have to register it at the top above every route with id.
Then we created productCarousel's component and exported it.
in productApiSlice we created getTopProducts query
we then use the component in the HomeScreen if no keyword we show the productCarousel component

