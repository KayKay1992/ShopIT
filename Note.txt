User Profile endpoint.

before getting the user profile we will first of all get the user.
then we check to see if we have a user else we set and error message.

then we also updated the user profile
in updateUserProfile functionality we first get the user,
then we update the user details 
and finally save the updated details.
then we returned an error message when unsuccessful.

try getting and updating a user profile in postman.

