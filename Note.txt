modelling our Data: 
we are going to create a model for our users, product, order etc, 
in backend we created a folder called models 
in the model folder thats where we will create our orderModel, productModel and userModel,
make sure you export the models created.